$(document).ready(function (){    $("#Apply_form").submit(function (event){        event.preventDefault();        var Data = new FormData(this)            // axax request        $.ajax({            url: '../Models/Apply.php',            type: 'POST',            data: Data,            dataType: 'json',            encode: true,            processData: false,            contentType: false,            success: function (res){                if (res.errors) {                    if (res.errors.Name) {                        $('#Name').addClass("is-invalid");                        $('#Name_msg').html(res.errors.Name).show()                    } else {                        $('#Name').removeClass("is-invalid");                        $('#Name_msg').html('').hide();                    }                     if (res.errors.Age) {                        $('#Age').addClass("is-invalid");                        $('#Age_msg').html(res.errors.Age).show()                    } else {                        $('#Age').removeClass("is-invalid");                        $('#Age_msg').html('').hide();                    }                      if (res.errors.Email) {                        $('#Email_add').addClass("is-invalid");                        $('#Email_add_msg').html(res.errors.Email).show()                    } else {                        $('#Email_add').removeClass("is-invalid");                        $('#Email_add_msg').html('').hide();                    }                      if (res.errors.Job) {                        $('#Select_job').addClass("is-invalid");                        $('#Select_job_msg').html(res.errors.Job).show()                    } else {                        $('#Select_job').removeClass("is-invalid");                        $('#Select_job_msg').html('').hide();                    }                   if (res.errors.Contact) {                        $('#Contact_number').addClass("is-invalid");                        $('#Contact_number_msg').html(res.errors.Contact).show()                    } else {                        $('#Contact_number').removeClass("is-invalid");                        $('#Contact_number_msg').html('').hide();                    }                    if (res.errors.Resume) {                        $('#upload_resume').addClass("is-invalid");                        $('#Resume_msg').html(res.errors.Resume).show()                    } else {                        $('#upload_resume').removeClass("is-invalid");                        $('#Resume_msg').html('').hide();                    }                }                if (res.status === 'success'){                    Swal.fire({                        title: res.message,                        confirmButtonColor: '#0D6EFD'                    });                    setTimeout(() =>{window.location.reload()},2000)                }            }        })    })})